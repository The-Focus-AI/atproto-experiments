# Example Sync Directory

This is a test directory to demonstrate the directory sync functionality.

The sync tool will:
1. Upload all files in this directory to the AT Protocol blob store
2. Create a manifest tracking all file locations
3. Allow you to download and restore the directory later
